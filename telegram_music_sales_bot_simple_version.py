"""
Previously the bot and server lived in one file. Code is now split into the `music_sales` package.

Run:
  python run_bot.py      # Telegram (polling)
  python run_server.py   # Flask + Stripe

Set environment variables as in .env.example (or load .env manually).
"""
